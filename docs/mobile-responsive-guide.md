# HanDoc Viewer 모바일 반응형 가이드

## 📱 반응형 브레이크포인트

```
Desktop (기본)     Tablet           Mobile
>768px            ≤768px           ≤480px
┌────────────┐    ┌──────────┐     ┌─────┐
│            │    │          │     │     │
│  A4 크기   │ →  │  100%    │  →  │ 100%│
│  210mm     │    │  너비    │     │ 너비│
│            │    │          │     │     │
└────────────┘    └──────────┘     └─────┘
```

## 🎨 주요 변경사항

### 1. 레이아웃

| 속성           | Desktop       | Tablet (≤768px) | Mobile (≤480px) |
|----------------|---------------|-----------------|-----------------|
| 페이지 너비    | 210mm (A4)    | 100%            | 100%            |
| 뷰어 패딩      | 20px          | 12px            | 8px             |
| 페이지 패딩    | 20mm/15mm     | 16mm/12mm       | 12px            |
| 배경색         | #e8e8e8       | #e8e8e8         | #fff (절약)     |
| 페이지 그림자  | 있음          | 있음            | 없음 (경계선)   |

### 2. 타이포그래피

| 요소           | Desktop       | Tablet          | Mobile          |
|----------------|---------------|-----------------|-----------------|
| 본문 폰트      | 1em           | 1.05em          | 1.1em           |
| 줄간격         | 기본          | 1.6             | 1.65            |
| 표 폰트        | 1em           | 0.95em          | 0.9em           |
| 각주/헤더      | 0.9em         | 0.9em           | 0.85em          |

### 3. 줌 컨트롤

| 속성           | Desktop       | Tablet          | Mobile          |
|----------------|---------------|-----------------|-----------------|
| 버튼 크기      | 36px          | 40px            | **44px** ✅     |
| Sticky         | ✅            | ✅              | ✅              |
| Zoom 기능      | ✅ (50-200%)  | ✅ (50-200%)    | ❌ (고정 100%)  |

> **Mobile Zoom 비활성화 이유:**  
> CSS `transform: scale()` 적용 시 작은 화면에서 가로 오버플로우 발생.  
> 대신 폰트/여백 최적화로 가독성 확보.

## 📊 표 (Table) 처리

### 기존 문제
```
┌─────────────────────────────────────────┐
│ 모바일 화면                              │ ← 표가 화면보다 넓음
│ ┌─────────────────────────────────────┐ │
│ │ 표 내용이 너무 넓어서 잘림...       →│ ← 스크롤 불가
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 개선 후
```
┌─────────────────────────────────────────┐
│ 모바일 화면                              │
│ ┌─────────────────────────────────────┐ │
│ │ 표 내용 전체가 가로 스크롤 ⟵────⟶ │ │ ✅
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

**구현:**
```html
<div class="handoc-table-wrapper"> <!-- 스크롤 컨테이너 -->
  <table class="handoc-table">
    <tr>
      <td style="min-width: 80px">...</td> <!-- 최소 너비 보장 -->
    </tr>
  </table>
</div>
```

```css
.handoc-table-wrapper {
  overflow-x: auto; /* 가로 스크롤 */
  -webkit-overflow-scrolling: touch; /* 부드러운 스크롤 */
}
```

## 🎯 터치 최적화

### 1. 최소 터치 타겟 크기 (44px)
```css
@media (max-width: 480px) {
  .handoc-zoom-btn {
    padding: 10px 12px;
    font-size: 16px;
    min-width: 44px; /* Apple HIG 기준 */
  }
}
```

### 2. Sticky 컨트롤
```css
.handoc-controls {
  position: sticky;
  top: 0;
  z-index: 100; /* 스크롤 시 상단 고정 */
}
```

### 3. 터치 피드백
```css
.handoc-zoom-btn {
  touch-action: manipulation; /* 더블탭 줌 방지 */
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0.1); /* 터치 피드백 */
}
```

## 📐 CSS 미디어 쿼리 구조

```css
/* 1. 기본 스타일 (Desktop) */
.handoc-page {
  width: 210mm;
  padding: 20mm 15mm;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
}

/* 2. 태블릿 (≤768px) */
@media (max-width: 768px) {
  .handoc-page {
    width: 100%; /* A4 고정 해제 */
    padding: 16mm 12mm;
  }
}

/* 3. 모바일 (≤480px) */
@media (max-width: 480px) {
  .handoc-page {
    padding: 12px; /* mm → px 단순화 */
    box-shadow: none; /* 그림자 제거 */
    border-bottom: 1px solid #e0e0e0; /* 구분선 */
  }
  
  .handoc-content {
    transform: none; /* 줌 비활성화 */
  }
}

/* 4. 인쇄 */
@media print {
  .handoc-controls {
    display: none; /* 줌 컨트롤 숨김 */
  }
}
```

## 🧪 테스트 방법

### Chrome DevTools 모바일 시뮬레이션

1. **개발자 도구 열기** (F12)
2. **Toggle device toolbar** (Ctrl+Shift+M / Cmd+Shift+M)
3. **디바이스 선택:**

```
iPhone SE        375 x 667   (작은 모바일)
iPhone 14 Pro    393 x 852   (중형 모바일)
iPad Mini        768 x 1024  (태블릿)
iPad Pro 12.9"  1024 x 1366  (큰 태블릿)
```

### 테스트 시나리오

#### ✅ 모바일 (480px 이하)
- [ ] 페이지 너비가 화면에 꽉 참
- [ ] 가로 스크롤바가 **표에서만** 나타남
- [ ] 줌 버튼 터치 시 44px 이상 크기
- [ ] 스크롤 시 줌 컨트롤이 상단 고정
- [ ] 폰트 크기가 읽기 편함 (1.1em)

#### ✅ 태블릿 (768px 이하)
- [ ] 페이지 너비 100% (A4 아님)
- [ ] 줌 기능 정상 작동 (50-200%)
- [ ] 표 가로 스크롤 가능

#### ✅ 데스크톱 (769px 이상)
- [ ] A4 크기 (210mm) 고정
- [ ] 좌우 회색 배경 (#e8e8e8)
- [ ] 페이지 그림자 표시

## 🎨 스타일 적용 예시

### React 컴포넌트
```tsx
import { HanDocViewer } from '@handoc/viewer';
import '@handoc/viewer/dist/styles.css'; // ← 반응형 CSS 포함

function App() {
  return (
    <HanDocViewer 
      buffer={hwpxBuffer}
      viewMode="continuous"
      showZoomControls={true} // 태블릿/데스크톱에서만 작동
    />
  );
}
```

### 모바일 우선 설계 원칙
1. **작은 화면부터 설계** → 큰 화면으로 확장
2. **필수 기능만 표시** → 옵션 기능은 숨김 가능
3. **터치 타겟 우선** → 마우스는 부가 기능

## 📊 성능 영향

| 항목            | 변경 전 | 변경 후 | 영향 |
|-----------------|---------|---------|------|
| CSS 파일 크기   | 3.8 KB  | 5.5 KB  | +45% (압축 시 무시 가능) |
| JS 번들 크기    | 14.87 KB| 14.87 KB| 변경 없음 ✅ |
| 렌더 시간       | -       | -       | 변경 없음 (CSS만 수정) |
| 테스트 통과율   | 32/32   | 40/40   | +8 테스트 추가 ✅ |

## 🔧 커스터마이징

### 브레이크포인트 변경
```css
/* 기본: 768px, 480px */
/* 변경하려면 styles.css 수정 */

@media (max-width: 640px) { /* 커스텀 브레이크포인트 */ }
```

### 폰트 크기 조정
```css
@media (max-width: 480px) {
  .handoc-para {
    font-size: 1.2em; /* 기본 1.1em → 1.2em */
  }
}
```

### 줌 컨트롤 항상 표시
```tsx
<HanDocViewer 
  buffer={buffer}
  showZoomControls={window.innerWidth > 480} // 조건부 표시
/>
```

## 🐛 알려진 제한사항

1. **모바일에서 Zoom 비활성화**
   - 이유: `transform: scale()`이 작은 화면에서 오버플로우 유발
   - 해결: 네이티브 브라우저 줌 사용 권장

2. **테이블 셀 최소 너비 (80px)**
   - 매우 좁은 셀도 80px 이상 차지
   - 의도: 탭하기 쉬운 터치 타겟 유지

3. **인쇄 시 미디어 쿼리 무시**
   - 브라우저가 `@media print` 우선 적용
   - 인쇄용 별도 CSS 필요 시 `@page` 사용

## 📚 참고 자료

- [Apple Human Interface Guidelines - Touch Targets](https://developer.apple.com/design/human-interface-guidelines/inputs/touch)
- [Material Design - Touch Target Size](https://m3.material.io/foundations/accessible-design/accessibility-basics)
- [MDN - Responsive Design](https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Responsive_Design)
- [Web.dev - Responsive Web Design Basics](https://web.dev/responsive-web-design-basics/)

---

**작성일:** 2026-02-21  
**버전:** 0.1.0  
**관련 태스크:** TASK-030
